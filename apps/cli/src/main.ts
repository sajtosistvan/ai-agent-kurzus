import { core } from '@plantbase/core';

// A2 váz: a CLI még üres, csak a core-ral való összekötést bizonyítja.
// A tényleges parancsok az A6-ban (üres CLI) és a B fázisokban épülnek.
console.log(`plantbase cli — core: ${core()}`);
